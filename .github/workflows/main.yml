# This is a basic workflow to help you get started with Actions

name: Deployment

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
env:
  SHOPIFY_PASSWORD: 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
  SHOPIFY_STORE_URL: dev039.myshopify.com
  SHOPIFY_NEW_THENE_NAME: theme_from_git
  
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    name: Building Code
    
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [16.x]

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        if: success()
        with:
          ref: ${{ steps.comment-branch.outputs.head_ref }}
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      - run: echo ${{ env.SHOPIFY_PASSWORD }} ${{ env.SHOPIFY_STORE_URL }}
      - run: git branch
      - run: npm ci
      - run: npm install
      #- run: sudo apt update && sudo apt dist-upgrade -y
      #- run: sudo apt install build-essential curl file git
      #- run: sudo apt install zsh
      #- name: Print github
      #  run: echo "$GITHUB_CONTEXT" && echo "$JOB_CONTEXT"
      #- run: npm run build --if-present
      #- run: sudo apt install ruby-full
      #- run: ruby --version
      #- run: sudo apt install git curl autoconf bison build-essential
      #- run: sudo gem install shopify-cli
      #- run: shopify version
      - run: curl -s https://shopify.dev/themekit.py | sudo python
      # - run: theme get --list -p=${{ env.SHOPIFY_PASSWORD }} -s="${{ env.SHOPIFY_STORE_URL }}"
      - run: theme new -p=${{ env.SHOPIFY_PASSWORD }} -s="${{ env.SHOPIFY_STORE_URL }}" --name=${{ env.SHOPIFY_NEW_THENE_NAME }}
      - run: theme deploy -p=${{ env.SHOPIFY_PASSWORD }} -s="${{ env.SHOPIFY_STORE_URL }}" --dir=./theme/
      - run: theme download templates/*.json config/* locales/* -p=${{ env.SHOPIFY_PASSWORD }} -s="${{ env.SHOPIFY_STORE_URL }}" --live
      - run: theme deploy -p=${{ env.SHOPIFY_PASSWORD }} -s="${{ env.SHOPIFY_STORE_URL }}" --dir=./theme/
      #- run: theme watch -p=${{ env.SHOPIFY_PASSWORD }} -s="${{ env.SHOPIFY_STORE_URL }}"
      #- run: shopify whoami