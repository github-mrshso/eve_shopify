{% comment %}
------------------------------------------------------------------------------
  Section: USP Icon Section PDP
  - A flexible section to display multiple USP items on the PDP.
------------------------------------------------------------------------------
{% endcomment %}

<link rel="stylesheet" href="{{ 'section-usp-icons.css' | asset_url }}">
<noscript>{{ 'section-usp-icons.css' | asset_url | stylesheet_tag }}</noscript>

{%- style -%}
  .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
      padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }
  @media screen and (min-width: 750px) {
      .section-{{ section.id }}-padding {
          padding-top: {{ section.settings.padding_top }}px;
          padding-bottom: {{ section.settings.padding_bottom }}px;
      }
  }
{%- endstyle -%}

{% for block in section.blocks %}
  {% if product.tags contains block.settings.title %}
    <section class="usp-icons color-{{ section.settings.color_scheme }} section-{{ section.id }}-padding">
      <div class="page-width usp-icons__inner">
        {% for i in (1..4) %}
          {%- liquid
            assign image = 'image_' | append: i
            assign title = 'title_' | append: i
            assign copy = 'copy_' | append: i
          -%}

          <div class="usp-icons__block text-center">
            <div class="usp-icons__image">
              <img src="{{ block.settings[image] }}" alt="{{ block.settings[image].alt }}">
            </div>
            <h3 class="usp-icons__title">
              {{ block.settings[title] }}
            </h3>
            <p class="usp-icons__copy">
              {{ block.settings[copy] }}
            </p>
          </div>
        {% endfor %}
      </div>
    </section>
    {% break %}
  {% endif %}
{% endfor %}
  
{% schema %}
{
  "name": "USP Icons",
  "class": "section-usp-icons",
  "settings": [
    {
      "type": "select",
      "id": "color_scheme",
      "options": [
        {
          "value": "accent-1",
          "label": "t:sections.footer.settings.color_scheme.options__1.label"
        },
        {
          "value": "accent-2",
          "label": "t:sections.footer.settings.color_scheme.options__2.label"
        },
        {
          "value": "background-1",
          "label": "t:sections.footer.settings.color_scheme.options__3.label"
        },
        {
          "value": "background-2",
          "label": "t:sections.footer.settings.color_scheme.options__4.label"
        },
        {
          "value": "inverse",
          "label": "t:sections.footer.settings.color_scheme.options__5.label"
        }
      ],
      "default": "background-1",
      "label": "t:sections.footer.settings.color_scheme.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
    "type": "section",
    "name": "Section",
    "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Tag name (for PDP)",
          "info": "To display the USP section on a specific PDP, tag a product with this tag name."
        },
        {
          "type": "header",
          "content": "Block 1"
        },
        {
          "type": "text",
          "id": "image_1",
          "label": "Image URL",
          "info": "Upload an SVG to the Files section of the admin and paste the url here."
        },
        {
          "type": "text",
          "id": "title_1",
          "label": "Title",
          "default": "Example block title"
        },
        {
          "type": "textarea",
          "id": "copy_1",
          "label": "Body copy",
          "default": "Example body copy"
        },
        {
          "type": "header",
          "content": "Block 2"
        },
        {
          "type": "text",
          "id": "image_2",
          "label": "Image URL",
          "info": "Upload an SVG to the Files section of the admin and paste the url here."
        },
        {
          "type": "text",
          "id": "title_2",
          "label": "Title",
          "default": "Example block title"
        },
        {
          "type": "textarea",
          "id": "copy_2",
          "label": "Body copy",
          "default": "Example body copy"
        },
        {
          "type": "header",
          "content": "Block 3"
        },
        {
          "type": "text",
          "id": "image_3",
          "label": "Image URL",
          "info": "Upload an SVG to the Files section of the admin and paste the url here."
        },
        {
          "type": "text",
          "id": "title_3",
          "label": "Title",
          "default": "Example block title"
        },
        {
          "type": "textarea",
          "id": "copy_3",
          "label": "Body copy",
          "default": "Example body copy"
        },
        {
          "type": "header",
          "content": "Block 4"
        },
        {
          "type": "text",
          "id": "image_4",
          "label": "Image URL",
          "info": "Upload an SVG to the Files section of the admin and paste the url here."
        },
        {
          "type": "text",
          "id": "title_4",
          "label": "Title",
          "default": "Example block title"
        },
        {
          "type": "textarea",
          "id": "copy_4",
          "label": "Body copy",
          "default": "Example body copy"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "USP Icons"
    }
  ],
  "templates": [
    "product"
  ]
}
{% endschema %}
  